function [multiplier] = springs_modifier(batch,multiplier,N_eq)
% Check which batch to use for plot

% Interim plot
batch.p = [1.45, 1.12 0.81; 1, 10, 1000];
batch.y = [1.2, 0.85 0.57; 1, 10, 1000];
% Interpolate 

end